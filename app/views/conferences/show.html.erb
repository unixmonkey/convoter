<p id="notice"><%= notice %></p>

<%= render @conference %>

<% @conference.daynames.each do |day| %>
  <div class='day'>
    <%= day %>
  </div>
<% end %>

<% if current_admin %>
  <%= link_to 'Edit', edit_conference_path(@conference) %> |
  <%= link_to 'Back', conferences_path %>
<% end %>
